partial : ({ r . s } -> a) -> r -> s -> a
let partial = \func -> \paramR -> \rest ->
  let myRec = { rest with paramR }
  in func myRec

let f = \r -> r.x + r.y

f { x = 1 }

1. f : (| x: Int, y: Int | p |) -> Int
2. { x = 1 } : (| x : Int |)
3: f { x = 1 } : (| y: Int | p |) -> Int

let addStuff = \r -> r.x + r.y

let add2ToY = \s -> partial addStuff { s with x = 2 }

let main = add2ToY { x = 1, y = 0 }
